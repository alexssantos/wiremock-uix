export { useDashboardMetrics } from "@/entities/dashboard-metrics/model/queries";
export type {
  DashboardMetrics,
  HttpMethodCount,
  RequestsPerMinutePoint,
  StatusCodeCount,
  TopUrlCount,
} from "@/entities/dashboard-metrics/model/types";
